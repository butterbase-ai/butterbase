# Copy to `.env.local`. These are baked into the client bundle at build time,
# so only put public identifiers here — never a service key.
VITE_BUTTERBASE_APP_ID=app_replace_me
VITE_BUTTERBASE_API_URL=https://api.butterbase.ai
VITE_BUTTERBASE_SUBDOMAIN=your-app-subdomain
