{
  "name": "ai-rag-query",
  "description": "Internal proxy for POST /rag/collections/{collection}/query. Exists because BUTTERBASE_INTERNAL_FN_KEY is explicitly rejected on RAG routes; DOs invoke this wrapper (which auths with its own BUTTERBASE_API_KEY) instead. Loopback/service_key only.",
  "triggers": [
    {
      "type": "http",
      "config": {
        "auth": "none",
        "method": "POST"
      },
      "enabled": true
    }
  ],
  "timeoutMs": 30000,
  "memoryLimitMb": 128,
  "envKeys": ["BUTTERBASE_API_KEY"]
}
