---
published_at: 2026-06-18T23:40:00Z
app_id: app_0ycj4ad7odud
visibility: public
listed: true
snapshot_id: 9c9876fc73af
selfclone_test: not_run
---

# Template publication

`butter-support` is published as a public, listed Butterbase template.

## Live URLs

- **Console:** https://butter-support.butterbase.dev
- **API base:** https://api.butterbase.ai/v1/app_0ycj4ad7odud
- **Widget bundle:** https://butter-support.butterbase.dev/widget.js
- **Favicon:** https://butter-support.butterbase.dev/favicon.svg

## Snapshot details

- Snapshot ID: `9c9876fc73af`
- Files: 78
- Compressed size: 510 KB
- Commit message: "v1 — butter-support recipe, commodity tier ready, deep tier extensible"
- Pushed: 2026-06-18T23:38:00Z

## What rides along on clone

- All source code under `frontend/`, `agents/`, root files (README, .butterbaseignore, etc.)
- Schema (20 tables) + all RLS policies (29)
- Functions (23) — function code is in the snapshot via `manage_function` history
- DurableObject class (`support-ticket-do`)
- RAG collection definition (empty — cloner ingests their own docs)
- Realtime config (6 tables)
- AI config (defaultModel, allowedModels, maxTokensPerRequest)
- Storage config (publicReadEnabled: false)
- Agent spec (`support-overview` re-imported from `agents/support-overview.json`)
- Seed rows for `support_skill` (default behavioral dimensions) and `autonomy_settings` (default = draft_for_approval) — marked `_seed: true`

## What does NOT ride along (cloner must do manually)

- Magic-link auth (works out of the box — no config needed)
- OAuth providers (none configured; not needed for magic-link)
- `BUTTERBASE_API_KEY` env vars on the 5 service functions + the DO — needs `auto_mint_api_key` config when running `manage_app clone` (see deferred F1)
- `SUBSTRATE_OUTBOX_SECRET` on `execute-escalation` — generated when registering the substrate outbox target
- Widget secret — generated by the cloner via `rotate-widget-secret` after first sign-in
- Documentation ingestion — cloner pastes their help-center URL in the setup wizard
- Escalation target — cloner connects Slack or Gmail via Composio in `/settings/integrations`
- Custom domain (optional)
- BYOK AI keys (optional — defaults to platform shared key)

## Self-clone test

Not run. The acceptance criteria are met (snapshot exists, repo bound, visibility public listed) but a real "clone into a fresh app and walk the wizard" test hasn't been done by me. **First-cloner UX is unverified.**

Recommendation: do a self-clone test before sharing the gallery link broadly:
```bash
butterbase clone app_0ycj4ad7odud /tmp/clone-test
cd /tmp/clone-test
# Follow README from a cold-cloner's perspective and report any gap.
```

## Update / republish flow

When you ship changes:
```bash
# Edit code, schema, etc.
butterbase repo status                  # see what changed
butterbase repo push --message "v1.1 — <change summary>"
```

Snapshot history visible via `butterbase repo log`.

## Open items before "production ready"

See `06-v1-deferred.md`. Most pressing for cloners:
- **F1** — `auto_mint_api_key` documentation for cloners is in the README but the clone tooling needs to be invoked with the right args. v2 of this snapshot should ship a `clone-config.json` helper.
- **DEP1** — Subdomain `/fn/` worker bug. Widget works (uses canonical API URL). Cloners hitting subdomain function URLs in their own code would see the bug; documented in README.
- **A1** — Auth hook signature verification. v1 trusts the post-auth payload; cloners should sign in immediately after clone to claim the first-user-owner slot before exposing the URL.
